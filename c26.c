#include <stdio.h>
int main()
{
    char c;
    printf("enter char:");
    scanf("%c",&c);
    if (c==a&e,i,o,u,A)
    //to do with if ,ifelse,else statements.
    {
        printf("vowel");
    }

    printf(");
    return 0;
}